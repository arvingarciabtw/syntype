export const EXAMPLE_ONE = `eval boolean asyncIterator concat eval this every any instanceOf abs function concat pop ngAfterContentInit then length continue while max metadata isFinite data import axios global round`;

export const EXAMPLE_TWO = `function calculateTotal(price, tax = 0.07) {
  const salesTax = price * tax;
  const total = price + salesTax;
  return total.toFixed(2);
}

const result = calculateTotal(100);
console.log(result);`;
